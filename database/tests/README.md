# database/tests

Database, migration, and schema-drift tests. Part of completion gates.

Populated per project by the `database-migration-builder`. Empty in the factory (this repo is not a product).
