# 30 — Codex Handoff (moved)

The canonical handoff is **`28-codex-handoff.md`** — the filename required by
`scripts/validate_plan_package.py`. This file is a pointer kept so earlier references resolve.
