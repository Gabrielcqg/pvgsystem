# 32 — Codex-Question Prediction Audit

**Question:** while executing this plan, is there any point at which Codex must stop and ask?

Every task in `23-task-decomposition.md` (Phases 0–6) was walked against five failure modes.

---

## Results

| Metric | Value |
|---|---|
| `unresolved_material_decisions` | **0** |
| `codex_questions_required` | **0** |
| `external_files_required_at_implementation_start` | **0** |
| `requirements_without_verification` | **0** (65 total: 64 automated + 1 evidence procedure) |

## Method

Mechanical scan for `TBD`/`TODO`/`decidir`/"Codex will decide"/"if you have"/"would unlock"/
"pending confirmation"/external `~/Downloads` paths, then a per-task reading of Phases 0–6.

Two residual `TBD by Gabriel` markers were found on the TJCE/TJBA rows of the integrations table and
replaced with `n/a — out of v1 scope (DEC-38)`. The one surviving `~/Downloads` mention is in the
handoff and is a **prohibition** ("do not search Downloads"), which is the intended content.

## Per-phase walk

| Phase | Could Codex need to ask? | Why not |
|---|---|---|
| **0 — Preserve** | No | Scraper + fixtures + golden output are **in the package** (`vendor/`, DEC-39). Canonical copy adjudicated during planning; hash recorded. T-004 runs first against a file that already exists. |
| **1 — Schema** | No | Every table, column, type, constraint, index and enum value is literal SQL. Migration order fixed (`000`→`010`, DEC-29). `000_bootstrap.sql` makes local Postgres sufficient (DEC-30). |
| **2 — Calculation** | No | All 23 formulas defined as-of a reference month (DEC-32). Per-table trigger month resolution tabulated. Historical vs point-in-time split resolves the rollover question (07 §6.1). |
| **3 — Domain/API** | No | Every endpoint has a method, path, body, status codes and write allowlist. The four transactional links are written out as pseudo-SQL including `NOT NULL` fields. |
| **4 — Radar** | No | Scraper contract, registry with reserved slots, orchestrator sequence, `CONCLUSIVO` set, `taxa_conclusiva` formula, run-status table incl. the NULL branch, vault provider + env names, `submeter_senha()` seam. |
| **5 — Hardening** | No | Five leak channels enumerated; `radar_worker` DDL with grant/policy symmetry; TEST-SEC-01…07 defined. |
| **6 — Loader** | No | Column aliases, validation severities, reconciliation rules, idempotency key, **synthetic fixtures generated by the plan**. No external file. |

## Five failure modes

### 1. Unresolved user questions — 0
All 41 decisions are recorded in `04-decision-ledger.md`. The two former open items are closed:
OPEN-01 → DEC-35 (region as validated config), OPEN-02 → DEC-37 (spreadsheets not required).

### 2. External file dependencies at implementation start — 0

| Formerly needed | Now |
|---|---|
| `consulta_tjsp_lote.py` from Downloads | **Vendored** at `vendor/scraper/`, sha256 `c9429f2a…` |
| HTML fixtures from `debug_tjsp/` | **Vendored**, 5 fixtures covering both protected branches |
| Validated run JSON | **Vendored** at `vendor/fixtures/` |
| Financial workbooks | **Not required** — Phase 6 uses synthetic fixtures (DEC-37) |
| Supabase project | **Not required to build** — local Postgres; EXT-DEP-01 gates production apply only |

### 3. Undefined configuration — 0
`AWS_REGION` (default + allowlist + validator), `RADAR_DB_URL`, `SUPABASE_*`, `VAULT_PROVIDER`,
`SUPABASE_VAULT_KEY_ID`, `SMTP_*`/`RESEND_API_KEY` — all named with consumer and validation rule
(17 §2.2). Runtime settings (`radar_cron`, `radar_email_erros`, `radar_inercia_dias`,
`radar_alerta_sem_rodada_dias`) are seeded rows in `configuracoes` with defaults.

### 4. Missing test evidence — 0
65 requirements: 64 automated tests, 1 (NFR-12) by the **EVID-01** evidence procedure with retained
artifacts. No dangling test IDs — every `TEST-*` cited anywhere is defined in `21`.

### 5. Decisions deferred to implementation — 0
Previously deferred and now closed: vault provider (DEC-27), API runtime identity (DEC-28), worker
identity (DEC-36), migration order (DEC-29), local bootstrap (DEC-30), tribunal vocabulary (DEC-31),
temporal semantics (DEC-32), balance-sheet treatment of overdue receivables (DEC-33), write
allowlists (DEC-34), region (DEC-35), spreadsheet classification (DEC-37), tribunal scope (DEC-38),
canonical scraper (DEC-39).

## The one external dependency — and why it is not a question

| ID | EXT-DEP-01 |
|---|---|
| What | Create the Supabase project in `sa-east-1`; supply `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `RADAR_DB_URL` |
| Owner | Gabriel — operator action on an external SaaS account |
| Blocks | Production apply; `GATE-PROD-RUN` |
| Blocks for Codex | **Nothing.** All seven phases build and verify on local Postgres. |
| Codex behavior | Complete all phases, stop before production apply, report readiness. **Do not wait. Do not ask.** |

This is a credential-provisioning act, not a design decision. It cannot be resolved by planning
because it requires account access — which is precisely why it is classified as an external
deployment dependency rather than an open question.

## Residual risks (accepted, not questions)

| Risk | Why not a question |
|---|---|
| Real workbook headers may differ from the aliases | Handled by `--validate-only` + a `COLUMN_ALIASES` data table (19 §6). Data operation, post-implementation. |
| Datacenter IP may raise captcha rates | `GATE-PROD-RUN` / EVID-01 measures it on the chosen host. Default is the validated Mac (DEC-23). |
| TJSP may change page layouts | TEST-SCRAPER-02/03 fail loudly on drift; `layout_movimentacoes` telemetry surfaces distribution shifts. |
| 12 TJCE/TJBA processes unmonitored | Deliberate v1 scope (DEC-38); visible and counted every run (FR-45). |
