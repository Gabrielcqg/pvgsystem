id: "UI-021"
purpose: "UI-F26 Auditoria - trilha somente-leitura"
route: "/auditoria"
user_role: ["member"]
user_objective: "trilha somente-leitura"
primary_action: "filtrar"
data_source: ["/auditoria"]
backend_dependency: ["/auditoria"]
permissions: ["member"]
states:
  default: "render normal com dados reais da API"
  loading: "skeleton no formato do layout final, sem spinner solto"
  empty: "dados carregados, zero linhas: explica o que criar + acao primaria"
  partial_data: "secoes com queries independentes carregam em paralelo"
  error: "erro de API/rede: mensagem + retry, estado local preservado"
  success: "render apos acao bem-sucedida com invalidacao de cache"
  permission_denied: "403 -> rota para UI-F04 (sem-acesso), nunca tabela em branco"
  disabled: "acoes desabilitadas quando sem permissao ou pre-condicao ausente"
responsive_behavior: "desktop-first; tabelas densas viram cards < md; acoes primarias sempre visiveis; periodo e totais visiveis"
accessibility: "labels, foco visivel, erros anunciados, navegacao por teclado"
persistence: "filtros e periodo persistidos por URL+store entre telas"
requirement_refs: ["FR-066"]
api_refs: ["/auditoria"]
acceptance_criteria: ["Renderiza dados reais da API; sem mock no build de producao", "Todos os estados (loading/empty/error/403/session) implementados"]
tests: ["TEST-FE-F26"]
